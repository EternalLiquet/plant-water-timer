spring:
  application:
    name: plant-care-api

app:
  version: ${APP_VERSION:0.1.0-SNAPSHOT}

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: ${SERVER_PORT:8080}
